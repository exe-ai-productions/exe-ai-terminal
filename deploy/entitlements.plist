<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<!-- The hardened runtime is a requirement for notarization. The bundled
     Python service needs these two doors left open: it maps its own
     libraries out of the unpacked bundle, and its FFI layer allocates
     executable memory. Without them the sidecar dies on launch. -->
<dict>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
