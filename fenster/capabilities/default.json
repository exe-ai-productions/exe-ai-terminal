{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The window itself; the sidecar is driven from Rust.",
  "windows": ["haupt"],
  "permissions": ["core:default"]
}
